#!/bin/bash
cd "$(dirname "$0")"/..
echo "HAL9000 Agent Status:"
echo "====================="
docker compose -f compose.yaml -p hal9000 ps
echo ""
docker compose -f compose.yaml -p hal9000 logs --tail=15 hal9000
