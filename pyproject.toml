[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hal9000"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["httpx"]

[project.scripts]
hal9000-host = "host.daemon:main"
